<section class="container">
  <h3 class="gallery-name">{{user.email}}'s Picks</h3>
  <section class="flex-row justify-center">
    {{#each user.picks as |pick|}}
    {{> pick-details}}
    {{/each}}
  </section>
</section>